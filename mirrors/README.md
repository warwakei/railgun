# Mirrors

Backup mirrors for Railgun dependencies.

## Structure

- `platform-tools/` — Android SDK Platform Tools
- `alpine/` — Alpine Linux minirootfs

These are used as fallback if primary sources are unavailable.
